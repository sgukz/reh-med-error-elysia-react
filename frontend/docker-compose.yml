services:
  rehMEDErrorApp:
    image: sgukz/react-app-mederror:1.10.16
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3016}:80"
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "1m"
        max-file: "1"
