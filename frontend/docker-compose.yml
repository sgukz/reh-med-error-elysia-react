services:
  rehMEDErrorApp:
    image: sgukz/react-app-mederror:1.25.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "127.0.0.1:3016:80"
    restart: always
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
